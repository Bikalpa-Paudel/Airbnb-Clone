ng1">{props.rating}</div>
                        <div className="country">({props.ratingNum}) • USA</div>
                    </div>
                    <div className="title">{props.title}</div>
                    <div className="price">From ${props.price}<span>/ person</span></div>
                </div>
            </section>
        </>
    )
}